var zerollupDemoSite1 = (function () {
    'use strict';

    var config$1 = {
        assetsUrl: '/'
    };

    config$1.assetsUrl = cnf.assetsUrl;
    // './config'

    var faceAngel = config$1.assetsUrl + "zerollup_demo_lib1/face_angel.png";

    // './some'

    function app(node) {
        console.log(node, config, faceAngel);
    }
    // './bootstrap'
    // './config'

    return app;

}());
//# sourceMappingURL=index.js.map
