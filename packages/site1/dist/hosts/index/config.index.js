var zerollupDemoSite1Config = (function () {
    'use strict';

    /** /: / **/
    var configBaseUrl = '/';
    var config = {
        some: 'index',
        configBaseUrl: configBaseUrl,
        assetsUrl: configBaseUrl + "/i"
    };

    return config;

}());
